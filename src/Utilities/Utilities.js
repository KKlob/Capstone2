class Utilities {

    getData(chamber) {
        // Returns Object to be placed in Redux State - Details in "Redux State Structure" of readme.md
        return `Getting ${chamber}`;
    }
}

export default Utilities;